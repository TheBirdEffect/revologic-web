import { styled } from "styled-components";
import { TextWrapper as Text } from "../../helpers/TextWrapper";

export const SectionHeadlineWrapper = styled(Text)`
    position: relative;
    top: -30px;
`;

